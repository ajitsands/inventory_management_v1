<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Direct all requests to index.php if physical file/folder doesn't exist
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
